# Gradle Settings
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Application Configuration
app.base.url=http://3.68.165.45
app.name=interview-test-app
app.version=1.0.0

# Test Configuration
test.thread.count=3
test.environment=TEST
test.logging.enabled=true
test.allure.enabled=true
test.retry.enabled=true
test.retry.count=3

# API Configuration
api.request.timeout=30000
api.retry.count=3
api.retry.delay=1000
api.connection.timeout=10000
api.socket.timeout=30000

# Environment URLs (can be overridden)
dev.base.url=http://dev.3.68.165.45
staging.base.url=http://staging.3.68.165.45
prod.base.url=http://3.68.165.45

# Test Data Configuration
test.data.valid.editor=supervisor
test.data.admin.editor=admin
test.data.invalid.editor=invalid_user

# Default Player Data
default.player.age=25
default.player.gender=MALE
default.player.role=user
default.player.screen.name=TestPlayer

# Reporting Configuration
allure.results.directory=build/allure-results
allure.report.directory=build/reports/allure

# Logging Configuration
logging.level.root=INFO
logging.level.com.interview.test=DEBUG
logging.level.io.restassured=DEBUG

# Browser Configuration (for future UI tests)
browser.default=chrome
browser.headless=true
browser.timeout=30

# Database Configuration (if needed)
db.url=jdbc:h2:mem:testdb
db.username=sa
db.password=
db.driver=org.h2.Driver